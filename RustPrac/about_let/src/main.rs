fn main() {

    // Place holder in Rust works very much similar to how the
    // printf works in Java and C.
    let x = 5;
    let y = 10;

    println!("x at the moment = {} and y and the moment = {}", x, y);
}
